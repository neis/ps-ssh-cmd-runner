# DEPRECATED: no longer read by the live collection path. The command-group
# catalog in lib/CollectorCore.ps1 (Get-CommandGroupCatalog) is now the source of
# truth for what the collector runs. Retained as the behavior-preservation
# reference for tests/CollectionPlanIntegration.Tests.ps1 (the 'full' profile is a
# superset of this list). Removal follow-up: when a cross-repo command-set parity
# check exists.
# Cisco IOS-XR commands (ASR9000, NCS, CRS)
# Paging disabled automatically via "terminal length 0"
show version
show inventory
show interfaces description
show interfaces
show ipv4 interface brief
show isis neighbors
show isis interface
show mpls ldp neighbor
show bgp summary
show bgp neighbors
show ospf neighbors
show ospf interface
show route
show route vrf all
show vrf all
show arp
show arp vrf all
show cdp neighbors detail
show lldp neighbors detail
show running-config
show platform
show redundancy summary
show environment
show logging
